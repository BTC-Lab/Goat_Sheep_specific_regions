These are the testing results for the local data samples and four extra WGS downloaded from the European Nucleotide Archive (ENA) browser
The calculation is done using the script calculate_coverage.sh
